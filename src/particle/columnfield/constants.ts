import type { ColumnFieldParams } from "./types";

export const DEFAULT_CONFIG: ColumnFieldParams = {
  palette: "cosmic",
  vizMode: "logSpiral",
  layoutShape: "disc",
  bloomIntensity: 1.5,
  bloomThreshold: 0.3,
  bloomRadius: 0.4,
  columnCount: 2500,
  discRadius: 5.0,
  cameraOrbitSpeed: 0.1,
  cameraDistance: 10.0,
  cameraHeight: 5.5,
};
