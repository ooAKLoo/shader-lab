export interface ColumnFieldParams {
  palette: string;
  vizMode: string;
  layoutShape: string;
  bloomIntensity: number;
  bloomThreshold: number;
  bloomRadius: number;
  columnCount: number;
  discRadius: number;
  cameraOrbitSpeed: number;
  cameraDistance: number;
  cameraHeight: number;
}
