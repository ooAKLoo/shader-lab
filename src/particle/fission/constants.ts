import type { FissionParams } from "./types";

export const DEFAULT_CONFIG: FissionParams = {
  splitCount: 5,
  speed: 1.0,
  particleSize: 40,
  scatterRadius: 150,
  color: "#4fc3f7",
  trailAlpha: 0.15,
  glowIntensity: 1.5,
};
