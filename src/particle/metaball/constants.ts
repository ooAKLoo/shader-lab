import type { MetaballParams } from "./types";

export const DEFAULT_CONFIG: MetaballParams = {
  splitCount: 5,
  speed: 1.0,
  particleSize: 40,
  scatterRadius: 150,
  color: "#ffffff",
  blur: 12,
  contrast: 20,
};
